"""Test suite for TokenTrim.

Tests use the standard-library ``unittest`` framework so they run without any
third-party packages (pytest, fastapi, openai, psycopg2 are NOT required):

    python -m unittest discover -s tests -t .

They also run under pytest if you have it installed.
"""
